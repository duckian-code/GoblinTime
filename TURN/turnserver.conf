apiVersion: v1
kind: ConfigMap
metadata:
  name: coturn-config
data:
  turnserver.conf: |
    listening-port=3478
    tls-listening-port=5349

    external-ip=172.20.255.200

    use-auth-secret
    static-auth-secret=$(COTURN_TURN_SECRET)

    realm=goblin-ti.me

    log-file=stdout
    verbose

    min-port=49152
    max-port=65535
