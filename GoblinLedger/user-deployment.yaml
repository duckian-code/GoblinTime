apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  labels:
    app: user-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
        - name: ledger-manager
          image: xpenguingodx/userservice:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8088
          env:
            - name: DB_URL
              valueFrom:
                secretKeyRef:
                  name: goblin-secrets
                  key: db-url
